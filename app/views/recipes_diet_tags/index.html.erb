<p id="notice"><%= notice %></p>

<h1>Listing Recipes Diet Tags</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes_diet_tags.each do |recipes_diet_tag| %>
      <tr>
        <td><%= link_to 'Show', recipes_diet_tag %></td>
        <td><%= link_to 'Edit', edit_recipes_diet_tag_path(recipes_diet_tag) %></td>
        <td><%= link_to 'Destroy', recipes_diet_tag, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipes diet tag', new_recipes_diet_tag_path %>
